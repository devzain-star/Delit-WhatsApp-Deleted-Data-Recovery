<resources >
    <!-- Base application theme. -->
    <style name="Base.Theme.Delit" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.AnimatedSplash.MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.Delit</item>
    </style>

    <style name="messageStyle">
        <item name="android:background">@drawable/message_bg_night</item>
    </style>

    <style name="chatStyle">
        <item name="cardBackgroundColor">@color/audio_night</item>
    </style>

    <style name="CustomImageViewStyle">
        <item name="android:tint">@color/white</item> 
    </style>
</resources>